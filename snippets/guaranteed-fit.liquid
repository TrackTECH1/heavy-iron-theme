{%- comment -%}
  Guaranteed Fit widget retired (2026-06-20) to remove the duplicate machine
  selector on the product page. Fitment is now captured solely by the required
  Make/Model gate in fitment-selector.liquid, which is INSIDE the product form
  and posts Machine Make / Machine Model / Track Size as line-item properties.

  buy-buttons.liquid is intentionally left untouched — its {% render 'guaranteed-fit' %}
  call now resolves to this no-op and outputs nothing.

  To restore the old widget, revert this file from the theme's version history.
{%- endcomment -%}
